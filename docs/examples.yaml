examples_readme:
  readme_template: docs/example_readme.md
  skip_examples:
    - sa-secret-rotation
  code_snippet_files:
    additional: []
  template_vars:
    production_considerations_comment: ""
    production_considerations: ""
    skip_if_name_contains: []
  versions_tf: {}
    # add: |
    #   provider "mongodbatlas" {}
    # skip_if_name_contains: []
    # generate_when_missing_only: false
    # force_generate: false

tables:
  - name: Examples
    columns: ["feature", "name"]
    link_column: name
    example_rows:
      - folder_name: existing-org
        feature: Existing Organization
        name: Manage Policies on an Existing Organization
      - folder_name: import
        feature: Import
        name: Import an Existing Organization into Terraform
      - folder_name: new-org-single-apply
        feature: New Organization
        name: Create Organization with Policies (Single Apply)
      - folder_name: new-org-two-step
        feature: New Organization
        name: Create Organization with Policies (Two-Step)
      - folder_name: sa-secret-rotation
        feature: Service Account
        name: Service Account Secret Rotation (Two-Secret Pattern)
        description: Rotates a standalone Atlas service account with two secrets, import bootstrap, staggered time_rotating schedules, and manual_changes triggers.
